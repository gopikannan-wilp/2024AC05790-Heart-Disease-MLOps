apiVersion: apps/v1
kind: Deployment
metadata:
  name: heart-disease-mlops-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: heart-disease-mlops-api
  template:
    metadata:
      labels:
        app: heart-disease-mlops-api
    spec:
      containers:
        - name: heart-disease-mlops-api
          image: heart-disease-mlops-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 20
